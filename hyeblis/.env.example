# Frontend environment variables
# Copy this file to .env.local and edit as needed.

# Set this when the built frontend is served from a host *different* to the Pi
# (e.g. you open the app on a laptop and the Pi is at raspberrypi.local).
# Leave blank to use the Vite dev-server proxy (local development).
# VITE_API_BASE_URL=http://raspberrypi.local:5000
